apiVersion: v1
kind: Service
metadata:
  name: simulator
  namespace: hospital-llm
spec:
  selector:
    app: simulator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
